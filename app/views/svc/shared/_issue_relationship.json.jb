json = issue_relationship.slice(:id, :target_id, :source_id, :member_id, :category, :created_at, :updated_at)

if issue_relationship.member
  json[:member] = render_schema "schema://svc/member", member: issue_relationship.member
end

if issue_relationship.target
  json[:target] = render_schema "schema://svc/issue", issue: issue_relationship.target
end

if issue_relationship.source
  json[:source] = render_schema "schema://svc/issue", issue: issue_relationship.source
end

json
