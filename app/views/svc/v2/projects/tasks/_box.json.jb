json = {
  task: render_schema("schema://svc/task", task: task)
}

if resource_graph_columns.include?('info')
  json['task_upshots'] = render_schema("schema://svc/task_upshot", collection: task.upshots, as: :task_upshot)
  json['issues'] = render_schema("schema://svc/issue", collection: task.issues, as: :issue)
end

json