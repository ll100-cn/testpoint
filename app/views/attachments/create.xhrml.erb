<%= render "info.html", attachment: @attachment %>
<% time = Time.now.to_i %>
<%= hidden_field_tag "task[task_attachments_attributes][#{time + @attachment.id}][attachment_id]", @attachment.id %>
<%= hidden_field_tag "issue[issue_attachments_attributes][#{time + @attachment.id}][attachment_id]", @attachment.id %>
<%= hidden_field_tag "comment[comment_attachments_attributes][#{time + @attachment.id}][attachment_id]", @attachment.id %>
