<div class="page-header">
  <h2><%= page_i18n :new, model_name: h(@issue_template) %></h2>
</div>

<%= bootstrap_horizontal_form_for [@project, @issue_template] do |f| %>
  <%= render "form", f: f %>
<% end %>
