<%= content_tag "div", data: { controller: "inline-edit", template: template } do %>
  <div class="d-flex">
    <span class="h5"><%=h Issue, attribute_name %></span>
    <span class="ml-auto"><%= link_to(content_tag(:i, '', class: 'far fa-edit'), "javascript:void(0)", data: { action: "click->inline-edit#replace" }) if can? :edit, issue %></span>
  </div>
  <%= yield %>
<% end %>
