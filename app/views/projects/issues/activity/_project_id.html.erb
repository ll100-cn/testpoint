<span>将</span><% state_names = Hash[Issue.state.options].invert %>
<span class="text-info"><%=h Issue, activity.property.to_sym %></span>
<span>从</span>
<span class="text-primary"><%= Project.find(activity.before_value).name %></span>
<span>修改为</span>
<span class="text-primary"><%= Project.find(activity.after_value).name %></span>
