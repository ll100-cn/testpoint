<div class="card-title bg-white">
  <%= image_tag creator.user.avatar_url, width: 30, class: "rounded-circle avatar mr-1" %>
  <%= creator.name %>
  <span class="ml-1 small text-muted"><%= create_or_edit_time_in_words(issue) %></span>
  <% if can? :edit, issue %>
    <div class="dropdown float-right dropdown-no-arrow">
      <button class="btn btn-sm dropdown-toggle" data-toggle="dropdown" style="background: transparent;">
        <i class="far fa-ellipsis-h" aria-hidden="true"></i>
      </button>
      <div class="dropdown-menu dropdown-menu-right" style="min-width: 3px;">
        <% data = { target: "#issue#{issue.id}_content", type: "xhrml", remote: true } %>
        <%= edit_link [project, issue], class: "small dropdown-item", data: data if can? :edit, issue %>
      </div>
    </div>
  <% end %>
</div>
