<div class="page-header">
  <%= destroy_link [ @project, @component ], class: "btn btn-outline-danger btn-sm float-right" %>
  <h2><%= page_i18n :edit, model_name: h(@component) %></h2>
</div>

<%= render "form", component: @component, project: @project %>
