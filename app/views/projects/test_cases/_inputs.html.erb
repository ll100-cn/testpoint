<%= f.input :title %>
<%= f.input :content, input_html: { rows: 5 } %>
<%= f.input :folder_id, collection: @project.folders.available.ranked %>
<%= f.input :platform_ids, collection: @project.platforms, as: :check_boxes %>
