<div class="row">
  <div class="col-md-12">
    <p><%=h User, :avatar %></p>
    <% if current_user.avatar.present? %>
      <%= link_to image_tag(current_user.avatar.url, size: "200x200")%>
    <% else %>
      <%= image_tag(current_user.avatar_url) %>
    <% end %>
  </div>
</div>